'use client'

import { CoachDesk } from '@/components/coach/desk/coach-desk'

/**
 * Coaching desk — Today is home. Coach is a tab (`?panel=coach`).
 */
export default function CoachDeskPage() {
  return <CoachDesk />
}
