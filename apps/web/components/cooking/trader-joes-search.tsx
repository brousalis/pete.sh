/**
 * @deprecated This component has been replaced by the unified RecipeList
 * component (recipe-list.tsx) which handles both own recipes and Trader Joe's
 * recipes, and TraderJoesDetailSheet (trader-joes-detail.tsx) for detail views.
 *
 * Kept for reference only.
 */
export {}
